import { FormatGeoPipe } from './format-geo.pipe';

describe('FormatGeoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatGeoPipe();
    expect(pipe).toBeTruthy();
  });
});
